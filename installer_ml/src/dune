(executable
 (name installer)
 (libraries core_kernel
            sexplib
            re
            file-utils)
 (flags (:standard -ccopt -static))
 (preprocess (pps ppx_sexp_conv
                  lwt_ppx)))
